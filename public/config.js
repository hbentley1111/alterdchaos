/* config.js — edit this to change your access code, then re-deploy.
   This file is NOT bundled, so you can open it in the deployed folder,
   change the code, and drag the folder back to Netlify — no rebuild needed.

   - Set a code to require it on the landing page.
   - Set it to "" (empty) to remove the passcode and just show an Enter button.

   Reminder: this is a deterrent, not real security. For true protection use
   the Netlify Edge Function (see netlify/edge-functions/gate.js) or Netlify
   Pro's dashboard Password Protection. */
window.__AC_PASSCODE__ = "altardchaos";
